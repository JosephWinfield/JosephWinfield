<%= form_for(@photo) do |f| %>
  <%= f.label :Photo %>
  <%= f.file_field :url, placeholder: "Upload photo" %>
  <br>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.submit "Add Photo" %>
<% end %>
